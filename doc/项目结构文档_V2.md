# 财报智能问数助手 V2 项目结构文档

## 技术栈

| 类别 | 技术 | 说明 |
|------|------|------|
| 后端框架 | FastAPI + Uvicorn | 构建高性能 REST API 服务 |
| 数据库 | MySQL (PyMySQL) | 关系型数据持久化存储 |
| ORM | SQLAlchemy | 数据库模型定义与查询操作 |
| 数据校验 | Pydantic / Pydantic Settings | 请求/响应 Schema 定义与环境变量管理 |
| 认证授权 | PyJWT + bcrypt + cryptography | JWT 令牌认证、密码哈希与安全处理 |
| LLM 调用 | LangChain + DashScope + OpenAI + Anthropic | 多模型 AI 调用，支持 Embedding 和 Chat |
| 向量数据库 | Milvus (PyMilvus) | 存储和检索文本向量 |
| 文件解析 | docx2txt + PyPDF + openpyxl + mutagen | 解析 Word、PDF、Excel、音频元数据 |
| 对象存储 | Alibaba Cloud OSS (oss2) | 文件上传至阿里云 OSS |
| 数据处理 | Pandas + Matplotlib | 数据分析与可视化 |
| 前端框架 | Vue 3 | 渐进式前端 UI 框架 |
| 路由 | Vue Router 4 | SPA 路由管理 |
| 状态管理 | Pinia | 全局状态管理 |
| HTTP 客户端 | Axios | 前端 HTTP 请求封装 |
| CSS 框架 | Tailwind CSS | 原子化 CSS 样式 |
| 图标 | Font Awesome (vue-fontawesome) | 界面图标展示 |
| Markdown | marked + DOMPurify | Markdown 渲染与 XSS 防护 |
| 构建工具 | Vite | 前端开发服务器与打包构建 |
| 包管理 | uv | Python 依赖管理与虚拟环境 |
| 运行时 | Python 3.12 | 后端运行环境 |

## 项目结构

```
project-root/
├── backendV2/                            # 后端 Python 服务
│   ├── .env                              # 环境变量配置（数据库连接、API Key 等）
│   ├── .python-version                   # Python 版本锁定（3.12）
│   ├── pyproject.toml                    # 项目元数据与依赖声明
│   ├── requirements.txt                  # Pip 依赖清单
│   └── app/
│       ├── main.py                       # FastAPI 应用入口，注册路由、中间件、异常处理
│       ├── core/
│       │   ├── config.py                 # 全局配置类（MySQL、Milvus、AI 模型、OSS 等配置）
│       │   └── prompt_config.py          # 提示词模板管理，从 YAML 加载四类 Prompt
│       ├── db/
│       │   └── database.py               # 数据库引擎、会话管理及事务辅助函数
│       ├── models/
│       │   ├── company_basic_info.py      # 公司基本信息 ORM 模型
│       │   ├── financial_report.py        # 财报主表 ORM 模型
│       │   ├── balance_sheet.py           # 资产负债表 ORM 模型
│       │   ├── cash_flow_sheet.py         # 现金流量表 ORM 模型
│       │   ├── income_sheet.py            # 利润表 ORM 模型
│       │   └── core_performance_indicators_sheet.py  # 核心业绩指标表 ORM 模型
│       ├── schemas/
│       │   ├── common.py                  # 通用 Schema（ApiResponse、PaginatedResponse、ErrorCode）
│       │   ├── response.py               # 统一响应构造函数（success/error）
│       │   └── import_company_base_info.py  # 公司信息导入响应 Schema
│       ├── constants/
│       │   └── import_company_base_info.py  # Excel 列名映射与交易所别名常量
│       ├── api/
│       │   └── import_company_base_info.py  # 公司信息导入 API 路由（POST /import）
│       ├── services/
│       │   └── import_company_base_info.py  # 公司信息导入业务逻辑（Excel 解析、数据插入/更新）
│       └── utils/
│           ├── exception.py              # 自定义服务层异常 ServiceException
│           ├── file.py                   # 文件操作工具（二进制保存、JSON 保存）
│           ├── logger_config.py          # 日志配置工具
│           └── model_factory.py          # AI 模型工厂（DashScope/OpenAI/Anthropic）
│
├── frontendV2/                           # 前端 Vue 3 应用
│   ├── index.html                        # SPA 入口 HTML
│   ├── package.json                      # 前端依赖声明与脚本配置
│   ├── jsconfig.json                     # JS 路径别名配置（@ → src/）
│   ├── vite.config.js                    # Vite 构建配置（Vue 插件、代理、端口 7488）
│   ├── postcss.config.js                 # PostCSS 配置（Tailwind + Autoprefixer）
│   ├── tailwind.config.js                # Tailwind 主题配置（色板、阴影、动画、字体）
│   └── src/
│       ├── main.js                       # 应用入口，注册 Pinia、Router、FontAwesome
│       ├── App.vue                       # 根组件，路由出口
│       ├── api/
│       │   ├── request.js                # Axios 封装（JWT 拦截器、FormData 处理、错误提取）
│       │   ├── company.js                # 公司导入 API（文件上传接口）
│       │   └── index.js                  # API 模块统一导出入口
│       ├── assets/
│       │   └── styles/
│       │       └── main.css              # 全局样式（CSS 变量、Tailwind 层叠、滚动条）
│       ├── components/
│       │   ├── ui/
│       │   │   ├── SurfacePanel.vue      # 通用卡片容器组件
│       │   │   └── AppEmptyState.vue     # 空状态展示组件
│       │   └── layout/
│       │       ├── AppShell.vue          # 布局壳组件（侧边栏 + 主内容区）
│       │       └── AppSidebar.vue        # 侧边栏导航（桌面固定 + 移动端底部 Tab）
│       ├── data/
│       │   └── navigation.js             # 导航菜单配置数据
│       ├── router/
│       │   └── index.js                  # Vue Router 路由配置
│       ├── stores/
│       │   └── index.js                  # Pinia Store 入口
│       └── views/
│           └── companies/
│               └── CompaniesImportView.vue  # 公司信息导入页面
│
├── doc/                                  # 项目文档目录
│   ├── B题-上市公司财报-智能问数助手.md      # 赛题描述文档
│   ├── 任务一设计思路说明.md               # 任务一设计思路
│   ├── 任务二设计思路说明.md               # 任务二设计思路
│   ├── 任务三设计思路说明.md               # 任务三设计思路
│   ├── API接口文档.md                     # API 接口文档
│   ├── 模块说明文档.md                    # 模块说明文档
│   ├── 项目结构文档.md                    # V1 项目结构文档
│   └── 项目结构文档_V2.md                 # 本文档
│
├── 规范文档/                              # 编码规范文档
│   └── 前端规范文档.md
│
├── CLAUDE.md                             # Claude Code 项目级指令
└── .gitignore                            # Git 忽略规则
```

### 依赖配置文件

| 文件 | 说明 |
|------|------|
| backendV2/pyproject.toml | 后端项目元数据，Python >= 3.12，通过 uv 管理依赖 |
| backendV2/requirements.txt | 后端 Pip 依赖清单（FastAPI、SQLAlchemy、LangChain 等 27 个包） |
| backendV2/.python-version | Python 版本锁定为 3.12 |
| frontendV2/package.json | 前端依赖声明，含 Vue 3、Pinia、Vue Router、Tailwind CSS、Axios 等 |
| frontendV2/package-lock.json | npm 依赖锁定文件 |
